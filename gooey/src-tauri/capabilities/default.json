{
  "$schema": "../node_modules/@tauri-apps/cli/schema/acl/capabilities.schema.json",
  "identifier": "default",
  "description": "Default permissions for the Gooey window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute",
    "shell:allow-spawn",
    "shell:allow-kill"
  ]
}
