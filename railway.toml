
[build]
providers = ["python"]
buildCommand = "mise install python@3.11.9 && mise use -g python@3.11.9 && python -m pip install -r gooey/requirements.txt"

[deploy]
startCommand = "cd gooey && python run.py --host 0.0.0.0 --port $PORT --no-browser"
